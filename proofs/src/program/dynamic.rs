//! This module will be for doing dynamic dispatch of circuits at runtime. E.g., read in a config
//! json and run the scheme there. This will be less performant than a strictly pre-compiled version
//! but will provide a convenient testing entrypoint
