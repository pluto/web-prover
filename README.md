# tlsn-monorepo

<!-- to run the notary server:

```
cargo run -p notary-server
``` -->

to run the go server:

```
cd tlsn/notary-server/
go run main.go
```

to run the client:

```
cargo run -p client 
```